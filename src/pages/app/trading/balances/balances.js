import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const TradingBalances = () => {
	return (
		<StyledWrapper>
			<div>TradingBalances</div>
		</StyledWrapper>
	)
}

export default TradingBalances