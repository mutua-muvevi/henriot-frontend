import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const TradingConfigure = () => {
	return (
		<StyledWrapper>
			<div>TradingConfigure</div>
		</StyledWrapper>
	)
}

export default TradingConfigure