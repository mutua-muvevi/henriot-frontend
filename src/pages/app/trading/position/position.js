import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const TradingPosition = () => {
	return (
		<StyledWrapper>
			<div>TradingPosition</div>
		</StyledWrapper>
	)
}

export default TradingPosition