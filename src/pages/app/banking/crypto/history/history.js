import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledCryptoHistory = styled(Box)(({ theme }) => ({

}));

const CryptoHistory = () => {
	return (
		<StyledCryptoHistory>
			<div>CryptoHistory</div>
		</StyledCryptoHistory>
	)
}

export default CryptoHistory