import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledTradingHistory = styled(Box)(({ theme }) => ({

}));

const TradingHistory = () => {
	return (
		<StyledTradingHistory>
			<div>TradingHistory</div>
		</StyledTradingHistory>
	)
}

export default TradingHistory