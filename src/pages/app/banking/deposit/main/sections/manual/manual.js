import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledTermDepositManual = styled(Box)(({ theme }) => ({

}));

const TermDepositManual = () => {
	return (
		<StyledTermDepositManual>
			TermDepositManual
		</StyledTermDepositManual>
	)
}

export default TermDepositManual