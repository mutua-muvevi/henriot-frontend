import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledTermDepositBorrow = styled(Box)(({ theme }) => ({

}));

const TermDepositBorrow = () => {
	return (
		<StyledTermDepositBorrow>
			TermDepositBorrow
		</StyledTermDepositBorrow>
	)
}

export default TermDepositBorrow