import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledWrapper = styled(Box)(({ theme }) => ({

}))

const TrustedContact = () => {
	return (
		<StyledWrapper>
			<div>TrustedContact</div>
		</StyledWrapper>
	)
}

export default TrustedContact